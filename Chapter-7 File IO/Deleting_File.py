#using the os module
"""
Module(like a code library) is a file written by another programmer that generally has a functions we can use.

import os
os.remove(filename)
"""
import os #no error
#import tensorflow -> given error if module don't have installed
# import tensorflow #pip/pip3(package installer for python) install tensorflow
os.remove("sample.txt")#remove sample.txt file